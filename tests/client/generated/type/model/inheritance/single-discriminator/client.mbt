///|
/// SingleDiscriminatorClient HTTP 客户端。
pub(all) struct SingleDiscriminatorClient {
  base_url : String
}

///|
/// 创建 SingleDiscriminatorClient。
pub fn SingleDiscriminatorClient::new(
  base_url : String,
) -> SingleDiscriminatorClient {
  { base_url, }
}

///|
/// 调用 get /type/model/inheritance/single-discriminator/model（自动解析响应）。
pub async fn SingleDiscriminatorClient::get_type_model_inheritance_single_discriminator_model(
  self : SingleDiscriminatorClient,
) -> Bird {
  let client = @http.Client::new(self.base_url)
  defer client.close()
  let headers : Map[String, String] = {}
  let base_path = @runtime.encode_request_target(
    "/type/model/inheritance/single-discriminator/model",
  )
  let sb = StringBuilder::new(size_hint=base_path.length() + 128)
  sb.write_string(base_path)
  let path = sb.to_string()
  client.request(@http.Get, path, extra_headers=headers)
  let response = client.end_request()
  if response.code == 200 {
    @runtime.expect_json_content_type(response)
    let response_body = client.read_all().text()
    let parsed : Bird = @json.from_json(@json.parse(response_body))
    parsed
  } else {
    fail("unexpected status code: " + response.code.to_string())
  }
}

///|
/// 调用 put /type/model/inheritance/single-discriminator/model（自动解析响应）。
pub async fn SingleDiscriminatorClient::put_type_model_inheritance_single_discriminator_model(
  self : SingleDiscriminatorClient,
  body : Bird,
) -> Unit {
  let client = @http.Client::new(self.base_url)
  defer client.close()
  let headers : Map[String, String] = {}
  let base_path = @runtime.encode_request_target(
    "/type/model/inheritance/single-discriminator/model",
  )
  let sb = StringBuilder::new(size_hint=base_path.length() + 128)
  sb.write_string(base_path)
  let path = sb.to_string()
  headers.set("content-type", "application/json")
  client.request(@http.Put, path, extra_headers=headers)
  client.write(body.to_json())
  let response = client.end_request()
  if response.code == 204 {
    ()
  } else {
    fail("unexpected status code: " + response.code.to_string())
  }
}

///|
/// 调用 get /type/model/inheritance/single-discriminator/recursivemodel（自动解析响应）。
pub async fn SingleDiscriminatorClient::get_type_model_inheritance_single_discriminator_recursivemodel(
  self : SingleDiscriminatorClient,
) -> Bird {
  let client = @http.Client::new(self.base_url)
  defer client.close()
  let headers : Map[String, String] = {}
  let base_path = @runtime.encode_request_target(
    "/type/model/inheritance/single-discriminator/recursivemodel",
  )
  let sb = StringBuilder::new(size_hint=base_path.length() + 128)
  sb.write_string(base_path)
  let path = sb.to_string()
  client.request(@http.Get, path, extra_headers=headers)
  let response = client.end_request()
  if response.code == 200 {
    @runtime.expect_json_content_type(response)
    let response_body = client.read_all().text()
    let parsed : Bird = @json.from_json(@json.parse(response_body))
    parsed
  } else {
    fail("unexpected status code: " + response.code.to_string())
  }
}

///|
/// 调用 put /type/model/inheritance/single-discriminator/recursivemodel（自动解析响应）。
pub async fn SingleDiscriminatorClient::put_type_model_inheritance_single_discriminator_recursivemodel(
  self : SingleDiscriminatorClient,
  body : Bird,
) -> Unit {
  let client = @http.Client::new(self.base_url)
  defer client.close()
  let headers : Map[String, String] = {}
  let base_path = @runtime.encode_request_target(
    "/type/model/inheritance/single-discriminator/recursivemodel",
  )
  let sb = StringBuilder::new(size_hint=base_path.length() + 128)
  sb.write_string(base_path)
  let path = sb.to_string()
  headers.set("content-type", "application/json")
  client.request(@http.Put, path, extra_headers=headers)
  client.write(body.to_json())
  let response = client.end_request()
  if response.code == 204 {
    ()
  } else {
    fail("unexpected status code: " + response.code.to_string())
  }
}

///|
/// 调用 get /type/model/inheritance/single-discriminator/missingdiscriminator（自动解析响应）。
pub async fn SingleDiscriminatorClient::get_type_model_inheritance_single_discriminator_missingdiscriminator(
  self : SingleDiscriminatorClient,
) -> Bird {
  let client = @http.Client::new(self.base_url)
  defer client.close()
  let headers : Map[String, String] = {}
  let base_path = @runtime.encode_request_target(
    "/type/model/inheritance/single-discriminator/missingdiscriminator",
  )
  let sb = StringBuilder::new(size_hint=base_path.length() + 128)
  sb.write_string(base_path)
  let path = sb.to_string()
  client.request(@http.Get, path, extra_headers=headers)
  let response = client.end_request()
  if response.code == 200 {
    @runtime.expect_json_content_type(response)
    let response_body = client.read_all().text()
    let parsed : Bird = @json.from_json(@json.parse(response_body))
    parsed
  } else {
    fail("unexpected status code: " + response.code.to_string())
  }
}

///|
/// 调用 get /type/model/inheritance/single-discriminator/wrongdiscriminator（自动解析响应）。
pub async fn SingleDiscriminatorClient::get_type_model_inheritance_single_discriminator_wrongdiscriminator(
  self : SingleDiscriminatorClient,
) -> Bird {
  let client = @http.Client::new(self.base_url)
  defer client.close()
  let headers : Map[String, String] = {}
  let base_path = @runtime.encode_request_target(
    "/type/model/inheritance/single-discriminator/wrongdiscriminator",
  )
  let sb = StringBuilder::new(size_hint=base_path.length() + 128)
  sb.write_string(base_path)
  let path = sb.to_string()
  client.request(@http.Get, path, extra_headers=headers)
  let response = client.end_request()
  if response.code == 200 {
    @runtime.expect_json_content_type(response)
    let response_body = client.read_all().text()
    let parsed : Bird = @json.from_json(@json.parse(response_body))
    parsed
  } else {
    fail("unexpected status code: " + response.code.to_string())
  }
}

///|
/// 调用 get /type/model/inheritance/single-discriminator/legacy-model（自动解析响应）。
pub async fn SingleDiscriminatorClient::get_type_model_inheritance_single_discriminator_legacy_model(
  self : SingleDiscriminatorClient,
) -> Dinosaur {
  let client = @http.Client::new(self.base_url)
  defer client.close()
  let headers : Map[String, String] = {}
  let base_path = @runtime.encode_request_target(
    "/type/model/inheritance/single-discriminator/legacy-model",
  )
  let sb = StringBuilder::new(size_hint=base_path.length() + 128)
  sb.write_string(base_path)
  let path = sb.to_string()
  client.request(@http.Get, path, extra_headers=headers)
  let response = client.end_request()
  if response.code == 200 {
    @runtime.expect_json_content_type(response)
    let response_body = client.read_all().text()
    let parsed : Dinosaur = @json.from_json(@json.parse(response_body))
    parsed
  } else {
    fail("unexpected status code: " + response.code.to_string())
  }
}
